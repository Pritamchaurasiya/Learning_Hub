## 2024-05-23 - Form UX & Accessibility
**Learning:** Adding `keyboardType`, `autofillHints`, and `textInputAction` to Flutter text fields provides a massive UX boost for mobile users with minimal code.
**Action:** Always check `TextFormField`s for these three properties. Don't forget `semanticsLabel` for loading indicators.
