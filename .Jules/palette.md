## 2024-05-22 - [Tooltips for Accessibility]
**Learning:** Icon-only buttons (like `IconButton`) in Flutter must explicitly define a `tooltip` property to ensure screen readers can announce the action effectively. This is a critical accessibility requirement for the project.
**Action:** Always verify `tooltip` is present when adding `IconButton` or similar icon-only widgets.
