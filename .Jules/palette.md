## 2026-02-05 - [Accessible Custom Icon Buttons]
**Learning:** Custom icon buttons using `Container` with color obscure `InkWell` ripples and lack accessibility.
**Action:** Use `Tooltip` > `Material` (color/shape) > `InkWell` > `Semantics` > `Icon` pattern.
