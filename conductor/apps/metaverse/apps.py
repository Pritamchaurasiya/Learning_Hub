from django.apps import AppConfig

class MetaverseConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.metaverse'
    verbose_name = 'Metaverse Spatial Learning'
