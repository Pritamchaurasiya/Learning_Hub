apiVersion: v1
kind: Service
metadata:
  name: {{ include "learning-hub.fullname" . }}
  labels:
    {{- include "learning-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "learning-hub.selectorLabels" . | nindent 4 }}
    component: backend
